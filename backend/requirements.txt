fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
SQLAlchemy>=2.0
alembic>=1.13
PyJWT[crypto]>=2.8
httpx>=0.27

# Agent provider. Only imported when AGENT_MODE=gemini.
google-genai>=1.0

# Dev/test only.
pytest>=8.0
psycopg[binary]>=3.1 ; sys_platform != "emscripten"
